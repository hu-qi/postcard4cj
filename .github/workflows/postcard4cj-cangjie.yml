name: postcard4cj pure Cangjie

on:
  push:
    branches:
      - main
    paths:
      - "src/**"
      - "cjpm.toml"
      - "**/*.rs"
      - "**/Cargo.toml"
      - "**/Cargo.lock"
      - "rust-toolchain*"
      - ".cargo/**"
      - ".github/scripts/setup-cangjie.sh"
      - ".github/scripts/resolve-cangjie-sdk-url.py"
      - ".github/workflows/postcard4cj-cangjie.yml"
  pull_request:
    paths:
      - "src/**"
      - "cjpm.toml"
      - "**/*.rs"
      - "**/Cargo.toml"
      - "**/Cargo.lock"
      - "rust-toolchain*"
      - ".cargo/**"
      - ".github/scripts/setup-cangjie.sh"
      - ".github/scripts/resolve-cangjie-sdk-url.py"
      - ".github/workflows/postcard4cj-cangjie.yml"
  workflow_dispatch:

permissions:
  contents: read

env:
  CANGJIE_VERSION: "1.1.3"
  CANGJIE_PLATFORM: "linux-x64"
  CANGJIE_CACHE_DIR: "${{ github.workspace }}/.cache/cangjie-sdk"
  CANGJIE_SDK_URL: "${{ vars.CANGJIE_SDK_URL }}"
  CANGJIE_SDK_SHA256: "${{ vars.CANGJIE_SDK_SHA256 }}"
  PYTHONUNBUFFERED: "1"

jobs:
  build-and-test:
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          show-progress: false

      - name: Enforce pure Cangjie sources
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t forbidden < <(
            find . -type f \
              \( -name '*.rs' -o -name 'Cargo.toml' -o -name 'Cargo.lock' -o -name 'rust-toolchain' -o -name 'rust-toolchain.toml' \) \
              -not -path './.git/*' -print
          )
          if (( ${#forbidden[@]} > 0 )); then
            printf 'Rust source/toolchain files are not allowed in postcard4cj:\n'
            printf ' - %s\n' "${forbidden[@]}"
            exit 1
          fi
          if [[ -d .cargo ]]; then
            echo '.cargo is not allowed in the pure Cangjie repository.'
            exit 1
          fi

      - name: Install native dependencies
        run: |
          sudo apt-get update --quiet=2
          sudo DEBIAN_FRONTEND=noninteractive apt-get install --yes --quiet=2 \
            binutils \
            libc6-dev \
            libc++-dev \
            libffi-dev \
            libssl-dev \
            libstdc++6 \
            zlib1g-dev >/dev/null

      - name: Cache Cangjie SDK
        uses: actions/cache@v4
        with:
          path: .cache/cangjie-sdk
          key: cangjie-sdk-${{ runner.os }}-${{ runner.arch }}-${{ env.CANGJIE_VERSION }}

      - name: Install Cangjie and run checks
        shell: bash
        run: |
          LOG_FILE="${RUNNER_TEMP}/postcard4cj-cangjie.log"
          set +e
          (
            set -euo pipefail
            source .github/scripts/setup-cangjie.sh
            cjpm build -V
            cjpm test -V
          ) >"${LOG_FILE}" 2>&1
          STATUS=$?
          cat "${LOG_FILE}"
          exit "${STATUS}"

      - name: Upload Cangjie diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: postcard4cj-cangjie-diagnostics
          path: ${{ runner.temp }}/postcard4cj-cangjie.log
          if-no-files-found: error
          retention-days: 7
